# Pinned deliberately. The calibration in calibration.json is fitted against a
# specific scikit-learn version; a floating pin let the serving model drift from
# the model the calibration was derived from, which shifted the raw score by up
# to 0.04 - roughly 1.4 in logit space at the fitted slope, enough to move a
# submission from "Low risk" to "High risk". Bump these together, and re-run
# `python train_model.py && python calibrate.py` when you do.
Flask==3.1.3
PyPDF2==3.0.1
joblib==1.5.3
scikit-learn==1.9.0
